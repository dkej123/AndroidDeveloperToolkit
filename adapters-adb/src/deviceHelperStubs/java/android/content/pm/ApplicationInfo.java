package android.content.pm;

/**
 * Compile-only stub of the platform class; the device provides the real one. {@code packageName} really
 * lives on the {@code PackageItemInfo} superclass, which field resolution walks at runtime.
 */
public class ApplicationInfo {
    public String packageName;
    public int flags;
}
